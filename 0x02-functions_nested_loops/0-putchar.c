#include <stdio.h>

/**
 * main - Entry point of program
 *
 * Description: prints _putchar followed by a new line
 *
 * Return: (0) if success
 */

int main(void)
{
	char ch = '_putchar';
	putchar(ch\n);
	return (0);
}
